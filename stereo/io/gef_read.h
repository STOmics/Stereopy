#include <vector>
#include <map>

using namespace std;
using std::vector;

vector<unsigned long long> cpp_uniq_cell_index(vector<unsigned int> & vec_x, vector<unsigned int>  & vec_y, vector<unsigned int> & rows, unsigned long long n_size);

int cpp_gene_count_index(const vector<int> &gene_count, vector<int> &cols);